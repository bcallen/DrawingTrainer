<UserControl x:Class="DrawingTrainer.Views.LibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:DrawingTrainer.Converters">
    <UserControl.Resources>
        <converters:FilePathToImageConverter x:Key="FilePathToImage"/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Photo Library" Style="{StaticResource PageHeader}" DockPanel.Dock="Left"/>
            <Button Content="Import Photos" Style="{StaticResource PrimaryButton}"
                    Command="{Binding NavigateToImportCommand}" DockPanel.Dock="Right"
                    HorizontalAlignment="Right"/>
        </DockPanel>

        <!-- Tag Filter -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Filter:" VerticalAlignment="Center" Margin="0,0,8,0"
                       Foreground="{StaticResource TextLightBrush}" FontSize="14"/>
            <Button Content="All" Style="{StaticResource TagToggleButton}" Margin="0,0,4,0"
                    Click="ClearFilter_Click"/>
            <ItemsControl ItemsSource="{Binding Tags}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Content="{Binding Name}" Style="{StaticResource TagToggleButton}" Margin="4,0"
                                Click="TagFilter_Click" Tag="{Binding}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>

        <!-- Tag Editing Bar (visible when photos are selected) -->
        <Border Grid.Row="2" Background="{StaticResource CardBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                CornerRadius="2" Padding="12" Margin="0,0,0,12"
                Visibility="{Binding HasSelection, Converter={StaticResource BoolToVis}}">
            <DockPanel>
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Edit Tags:" FontSize="14" FontWeight="SemiBold"
                               VerticalAlignment="Center" Margin="0,0,12,0"
                               Foreground="{StaticResource TextBrush}"/>
                    <ItemsControl ItemsSource="{Binding TagAssignments}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Margin="3,0" Padding="12,6" FontSize="13" Cursor="Hand"
                                        Command="{Binding DataContext.ToggleTagOnSelectionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}">
                                    <Button.Content>
                                        <TextBlock Text="{Binding Tag.Name}"/>
                                    </Button.Content>
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border x:Name="Bd" CornerRadius="2"
                                                                Padding="{TemplateBinding Padding}"
                                                                Background="{StaticResource BackgroundBrush}"
                                                                BorderBrush="{StaticResource BorderBrush}"
                                                                BorderThickness="1">
                                                            <ContentPresenter HorizontalAlignment="Center"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <DataTrigger Binding="{Binding IsAssigned}" Value="True">
                                                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                                                <Setter Property="Foreground" Value="White"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding IsPartial}" Value="True">
                                                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentBrush}"/>
                                                                <Setter Property="Foreground" Value="Black"/>
                                                            </DataTrigger>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter TargetName="Bd" Property="Opacity" Value="0.85"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>

                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal"
                            HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Content="Select All" Style="{StaticResource SecondaryButton}"
                            Padding="10,5" FontSize="12" Margin="0,0,4,0"
                            Command="{Binding SelectAllCommand}"/>
                    <Button Content="Clear" Style="{StaticResource SecondaryButton}"
                            Padding="10,5" FontSize="12"
                            Command="{Binding ClearSelectionCommand}"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Photo Grid -->
        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Photos}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="4" Width="200" Cursor="Hand"
                                MouseLeftButtonDown="Photo_Click">
                            <Border.Style>
                                <Style TargetType="Border" BasedOn="{StaticResource Card}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                            <Setter Property="BorderThickness" Value="2"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="150"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Selection Checkbox (visual only, click handled by card) -->
                                <CheckBox Grid.Row="0" IsChecked="{Binding IsSelected, Mode=OneWay}"
                                          IsHitTestVisible="False" Focusable="False"
                                          HorizontalAlignment="Left" VerticalAlignment="Top"
                                          Margin="4,4,0,0" Panel.ZIndex="1"/>

                                <Image Source="{Binding Photo.FilePath, Converter={StaticResource FilePathToImage}, ConverterParameter=200, IsAsync=True}"
                                       Stretch="UniformToFill" Grid.Row="0">
                                    <Image.Clip>
                                        <RectangleGeometry Rect="0,0,168,150" RadiusX="4" RadiusY="4"/>
                                    </Image.Clip>
                                </Image>

                                <DockPanel Grid.Row="1" Margin="0,8,0,0">
                                    <TextBlock Text="{Binding Photo.OriginalFileName}" FontSize="12"
                                               Foreground="{StaticResource TextLightBrush}"
                                               TextTrimming="CharacterEllipsis" DockPanel.Dock="Left"
                                               VerticalAlignment="Center" MaxWidth="130"/>
                                    <Button Content="X" FontSize="10" Padding="6,3" DockPanel.Dock="Right"
                                            HorizontalAlignment="Right" Style="{StaticResource DangerButton}"
                                            Command="{Binding DataContext.DeletePhotoCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"/>
                                </DockPanel>

                                <TextBlock Grid.Row="2" Text="{Binding TagNames}" FontSize="11"
                                           Foreground="{StaticResource TextLightBrush}" Margin="0,4,0,0"
                                           TextTrimming="CharacterEllipsis"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Empty State -->
        <TextBlock Grid.Row="3" Text="No photos yet. Click 'Import Photos' to get started!"
                   FontSize="16" Foreground="{StaticResource TextLightBrush}"
                   HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Photos.Count}" Value="0">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <!-- Loading -->
        <TextBlock Grid.Row="3" Text="Loading..." FontSize="16" Foreground="{StaticResource TextLightBrush}"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"/>
    </Grid>
</UserControl>
