<UserControl x:Class="DrawingTrainer.Views.PhotoImportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:DrawingTrainer.Converters">
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Import Photos" Style="{StaticResource PageHeader}"/>

        <!-- File Selection -->
        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="Select Photos" Style="{StaticResource SectionHeader}"/>
                <StackPanel Orientation="Horizontal">
                    <Button Content="Select Files" Style="{StaticResource PrimaryButton}"
                            Command="{Binding SelectFilesCommand}" Margin="0,0,8,0"/>
                    <Button Content="Select Folder" Style="{StaticResource SecondaryButton}"
                            Command="{Binding SelectFolderCommand}"/>
                </StackPanel>
                <TextBlock Text="{Binding StatusText}" Margin="0,8,0,0"
                           Foreground="{StaticResource TextLightBrush}" FontSize="13"/>
            </StackPanel>
        </Border>

        <!-- File List -->
        <Border Grid.Row="2" Style="{StaticResource Card}" Margin="0,0,0,16"
                MaxHeight="150"
                Visibility="{Binding SelectedFiles.Count, Converter={StaticResource BoolToVis}, FallbackValue=Collapsed}">
            <StackPanel>
                <TextBlock Text="Selected Files" FontWeight="SemiBold" Margin="0,0,0,8" FontSize="14"
                           Foreground="{StaticResource TextBrush}"/>
                <ScrollViewer MaxHeight="100" VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding SelectedFiles}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding}" FontSize="12"
                                           Foreground="{StaticResource TextLightBrush}" Margin="0,2"/>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </StackPanel>
        </Border>

        <!-- Tag Selection -->
        <Border Grid.Row="3" Style="{StaticResource Card}" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="Assign Tags" Style="{StaticResource SectionHeader}"/>
                <TextBlock Text="Select one or more categories for these photos:" FontSize="13"
                           Foreground="{StaticResource TextLightBrush}" Margin="0,0,0,12"/>
                <ItemsControl ItemsSource="{Binding Tags}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ToggleButton Content="{Binding Name}" Margin="4" Padding="12,6"
                                          FontSize="13" Cursor="Hand"
                                          Click="TagToggle_Click" Tag="{Binding}">
                                <ToggleButton.Style>
                                    <Style TargetType="ToggleButton">
                                        <Setter Property="Background" Value="{StaticResource BackgroundBrush}"/>
                                        <Setter Property="Foreground" Value="{StaticResource TextLightBrush}"/>
                                        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="ToggleButton">
                                                    <Border Background="{TemplateBinding Background}"
                                                            BorderBrush="{TemplateBinding BorderBrush}"
                                                            BorderThickness="1" CornerRadius="2"
                                                            Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="Center"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsChecked" Value="True">
                                                            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                                            <Setter Property="Foreground" Value="White"/>
                                                        </Trigger>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter Property="Opacity" Value="0.9"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ToggleButton.Style>
                            </ToggleButton>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>

        <!-- Progress -->
        <StackPanel Grid.Row="4" VerticalAlignment="Top"
                    Visibility="{Binding IsImporting, Converter={StaticResource BoolToVis}}">
            <ProgressBar Value="{Binding ImportProgress}" Maximum="{Binding ImportTotal}"
                         Height="8" Margin="0,0,0,8" Foreground="{StaticResource PrimaryBrush}"/>
            <TextBlock Text="{Binding StatusText}" FontSize="13"
                       Foreground="{StaticResource TextLightBrush}" HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- Actions -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Style="{StaticResource SecondaryButton}"
                    Command="{Binding CancelCommand}" Margin="0,0,8,0"/>
            <Button Content="Import" Style="{StaticResource PrimaryButton}"
                    Command="{Binding ImportCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
