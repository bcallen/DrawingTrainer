<UserControl x:Class="DrawingTrainer.Views.ActiveSessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:DrawingTrainer.Converters"
             xmlns:vm="clr-namespace:DrawingTrainer.ViewModels"
             xmlns:views="clr-namespace:DrawingTrainer.Views"
             Focusable="True" KeyDown="OnKeyDown">
    <UserControl.Resources>
        <converters:FilePathToImageConverter x:Key="FilePathToImage"/>
        <converters:BoolToVisibilityConverter x:Key="BoolToVis"/>
        <converters:EnumToVisibilityConverter x:Key="EnumToVis"/>
    </UserControl.Resources>

    <Grid Background="{StaticResource BackgroundBrush}">
        <!-- Drawing State -->
        <Grid Visibility="{Binding State, Converter={StaticResource EnumToVis}, ConverterParameter=Drawing}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Top Bar -->
            <Border Grid.Row="0" Background="{StaticResource CardBrush}"
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1"
                    Padding="16,12">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
                        <TextBlock Text="{Binding CategoryName}" FontSize="18" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextBrush}" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding ExerciseInfo}" FontSize="14"
                                   Foreground="{StaticResource TextLightBrush}"
                                   VerticalAlignment="Center" Margin="16,0,0,0"/>
                    </StackPanel>

                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                        <TextBlock Text="{Binding TimerText}" FontSize="36" FontWeight="Bold"
                                   Foreground="{StaticResource PrimaryBrush}" VerticalAlignment="Center"
                                   FontFamily="Consolas"/>
                    </StackPanel>
                </DockPanel>
            </Border>

            <!-- Timer Progress Bar -->
            <ProgressBar Grid.Row="0" VerticalAlignment="Bottom" Height="4"
                         Value="{Binding TimerProgress}" Maximum="1" Minimum="0"
                         Foreground="{StaticResource PrimaryBrush}" Background="Transparent"
                         BorderThickness="0"/>

            <!-- Reference Photo -->
            <views:ZoomableImage Grid.Row="1" Margin="16"
                                 Source="{Binding CurrentPhotoPath, Converter={StaticResource FilePathToImage}, ConverterParameter=1200}"/>

            <!-- No Photo Available -->
            <TextBlock Grid.Row="1" Text="No reference photos available for this category"
                       FontSize="18" Foreground="{StaticResource TextLightBrush}"
                       HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding CurrentPhotoPath}" Value="">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <!-- Bottom Controls -->
            <Border Grid.Row="2" Background="{StaticResource CardBrush}"
                    BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0"
                    Padding="16,12">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Style="{StaticResource SecondaryButton}" Command="{Binding PauseResumeCommand}"
                            Padding="24,10" Margin="0,0,8,0">
                        <Button.Content>
                            <TextBlock>
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="Pause"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsPaused}" Value="True">
                                                <Setter Property="Text" Value="Resume"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Button.Content>
                    </Button>
                    <Button Content="Skip" Style="{StaticResource SecondaryButton}"
                            Command="{Binding SkipCommand}" Padding="24,10" Margin="0,0,8,0"/>
                    <Button Content="End Session" Style="{StaticResource DangerButton}"
                            Command="{Binding EndSessionCommand}" Padding="24,10"/>
                </StackPanel>
            </Border>

            <!-- Pause Overlay -->
            <Border Grid.Row="0" Grid.RowSpan="3"
                    Background="#AA000000"
                    Visibility="{Binding IsPaused, Converter={StaticResource BoolToVis}}">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="PAUSED" FontSize="48" FontWeight="Bold" Foreground="White"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="Press Space or click Resume to continue" FontSize="16"
                               Foreground="#AAA" HorizontalAlignment="Center" Margin="0,12,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Break State -->
        <Grid Visibility="{Binding State, Converter={StaticResource EnumToVis}, ConverterParameter=Break}">
            <Border Background="#1A8B5CF6">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <TextBlock Text="BREAK" FontSize="48" FontWeight="Bold"
                               Foreground="{StaticResource BreakBrush}" HorizontalAlignment="Center"/>
                    <TextBlock Text="Take a photo of your drawing!" FontSize="20"
                               Foreground="{StaticResource TextBrush}" HorizontalAlignment="Center"
                               Margin="0,12,0,0"/>
                    <TextBlock Text="{Binding TimerText}" FontSize="64" FontWeight="Bold"
                               Foreground="{StaticResource BreakBrush}" HorizontalAlignment="Center"
                               Margin="0,24,0,0" FontFamily="Consolas"/>
                    <TextBlock Text="Next exercise starting soon..." FontSize="16"
                               Foreground="{StaticResource TextLightBrush}" HorizontalAlignment="Center"
                               Margin="0,12,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
